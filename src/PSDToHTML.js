//this file is purely used as a bootstrapper for
//all files


const PSD = require('psd');
const util = require('./psdtohtml/util');

module.exports = function () {
    const utils = new util();


    this.convert = function convert(filePath, config = {}) {
    }

}
